= form_for movie do |f|
  - if movie.errors.any?
    .movie-form__errors
      %h2
      = "#{@movie.errors.full_messages.count}件のエラーがあります"
      %ul
        - movie.errors.full_messages.each do |error|
          %li
          = error
  .movie-form
    .movie-form__field
      = f.label :name, '動画名:', class: '.movie-form__field__movie-label'
      = f.text_field :name, placeholder: '動画名を入力して下さい', class: 'movie-form__field__movie-name'
    .movie-form__user
      %label.movie-form__user__label 追加メンバー:
      %input.movie-form__user__input{type: 'text', placeholder: 'メンバーを入力して下さい'}
    .movie-form__result

    .movie-form__member
      %label.movie-form__member__name-label メンバー:
      .movie-form__member__list
        .movie-form__member__list__lists
          %input{name: 'movie[user_ids][]', value: current_user.id, type: 'hidden'}
          %p.member
            = current_user.nickname
          .self-btn あなた

        - movie.users.each do |user|
          - if current_user.nickname != user.nickname
            .movie-form__member__list__lists
              %input{name: 'movie[user_ids][]', value: user.id, type: 'hidden'}
              %p.member
                = user.nickname
              .remove-btn 削除

    .movie-form__submit
      = f.submit '保存', class: '.movie-form__submit__btn'

  